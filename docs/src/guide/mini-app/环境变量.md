# 环境变量

```js
// 获取当前小程序的账号信息
const {
    miniProgram
} = wx.getAccountInfoSync();

// 获取小程序版本，develop（开发版）、trial（体验版）、release（正式版）
const {
    envVersion
} = miniProgram;

let env = {

}

switch (envVersion) {
    case develop:
        env.xxx = 'xxx'
        break;
    case trial:

        break;
    case release:

        break;
    default:
        break;
}

export {
    env
}
```